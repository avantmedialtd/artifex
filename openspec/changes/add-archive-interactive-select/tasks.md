# Tasks

- [ ] Rename `ApplyChangeSelect` component to `ChangeSelect` for generic reuse
- [ ] Rename `apply-select.tsx` to `change-select.tsx`
- [ ] Rename `renderApplyChangeSelect` to `renderChangeSelect`
- [ ] Rename `apply-select-render.tsx` to `change-select-render.tsx`
- [ ] Update `handleSpecApply` to use renamed imports
- [ ] Update `handleSpecArchive` to use interactive selection logic
- [ ] Add component prop for customizing the prompt text (e.g., "Select a change to apply:" vs "Select a change to archive:")
- [ ] Run tests and verify both apply and archive commands work correctly

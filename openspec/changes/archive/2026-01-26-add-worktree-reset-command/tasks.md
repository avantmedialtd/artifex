# Tasks

## 1. Implementation

- [x] 1.1 Add `handleWorktreeReset` function to `commands/worktree.ts`
- [x] 1.2 Add routing for `worktree reset [name]` subcommand in `handleWorktree`
- [x] 1.3 Update help text in `commands/help.ts` for worktree command

## 2. Testing

- [x] 2.1 Add unit tests for worktree reset functionality
- [x] 2.2 Test edge cases (not in repo, uncommitted changes, invalid worktree name)

## 3. Validation

- [x] 3.1 Run `bun run format:check` to verify formatting
- [x] 3.2 Run `bun run lint` to verify linting passes
- [x] 3.3 Run `bun test` to verify all tests pass

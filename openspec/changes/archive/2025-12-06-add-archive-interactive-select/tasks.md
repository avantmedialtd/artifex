# Tasks

- [x] Rename `ApplyChangeSelect` component to `ChangeSelect` for generic reuse
- [x] Rename `apply-select.tsx` to `change-select.tsx`
- [x] Rename `renderApplyChangeSelect` to `renderChangeSelect`
- [x] Rename `apply-select-render.tsx` to `change-select-render.tsx`
- [x] Update `handleSpecApply` to use renamed imports
- [x] Update `handleSpecArchive` to use interactive selection logic
- [x] Add component prop for customizing the prompt text (e.g., "Select a change to apply:" vs "Select a change to archive:")
- [x] Run tests and verify both apply and archive commands work correctly

{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Install extension",
            "command": "cd vscode-extension/; npm install; npm run compile; npm run package; npm run install-extension",
            "problemMatcher": []
        }
    ]
}